import 'package:flutter/material.dart';

class OnBordaingScreen extends StatelessWidget {
  const OnBordaingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
